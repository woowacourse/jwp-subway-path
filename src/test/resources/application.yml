spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  server:
    tomcat:
      uri-encoding: UTF-8