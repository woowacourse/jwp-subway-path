spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:subway;MODE=MySQL
    driver-class-name: org.h2.Driver
  sql:
    init:
      data-locations: classpath:truncate.sql
      schema-locations: classpath:schema.sql
      mode: always
