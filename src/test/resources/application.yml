spring:
  h2:
    console:
      enabled: false
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/test;MODE=MySQL
    username: sa
    password:
  sql:
    init:
      mode: always
