spring:
  profiles:
    active: prod
---
spring:
  profile: prod
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:tcp://localhost/~/subway
    driver-class-name: org.h2.Driver
---
spring:
  profile: test
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:subway;MODE=MySQL
    driver-class-name: org.h2.Driver